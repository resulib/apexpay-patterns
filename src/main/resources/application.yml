server:
  port: 8084

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/apexpay_db
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true